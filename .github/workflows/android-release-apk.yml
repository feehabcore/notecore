name: Build And Release Android APK

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (example: v1.0.0)"
        required: false
        default: ""
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Install dependencies
        run: npm ci

      - name: Build web app
        run: npm run build

      - name: Sync Capacitor Android
        run: npx cap sync android

      - name: Ensure Gradle wrapper executable
        run: chmod +x android/gradlew

      - name: Build debug APK
        working-directory: android
        run: ./gradlew --no-daemon assembleDebug

      - name: Resolve release tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ github.event.inputs.tag }}" ]; then
            echo "TAG_NAME=${{ github.event.inputs.tag }}" >> $GITHUB_ENV
          else
            echo "TAG_NAME=${{ github.ref_name }}" >> $GITHUB_ENV
          fi
        shell: bash

      - name: Rename APK
        run: |
          cp android/app/build/outputs/apk/debug/app-debug.apk "notecore-${TAG_NAME}-debug.apk"
          cp android/app/build/outputs/apk/debug/app-debug.apk "notecore-latest-debug.apk"
        shell: bash

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.TAG_NAME }}
          name: Notecore ${{ env.TAG_NAME }}
          body: |
            Android APK build for Notecore.

            Install steps:
            1. Download `notecore-${{ env.TAG_NAME }}-debug.apk`
            2. Or use stable asset name: `notecore-latest-debug.apk`
            2. Open on Android
            3. Allow "Install unknown apps" if prompted
          files: |
            notecore-${{ env.TAG_NAME }}-debug.apk
            notecore-latest-debug.apk
          fail_on_unmatched_files: true
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload renamed APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: notecore-${{ env.TAG_NAME }}-debug-apk
          path: |
            notecore-${{ env.TAG_NAME }}-debug.apk
            notecore-latest-debug.apk
          if-no-files-found: error

